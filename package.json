
{
  "name": "calcsimpler",
  "version": "0.36.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "astro dev",
    "build": "astro build",
    "preview": "astro preview",
    "generate": "node --no-warnings --loader ts-node/esm scripts/generate_calcs.ts",
    "postbuild:check": "node scripts/check_links.mjs",
    "sitemap:ping": "node scripts/ping_sitemaps.mjs"
  },
  "dependencies": {
    "astro": "^4.10.0",
    "@astrojs/mdx": "^3.1.0",
    "@astrojs/sitemap": "^3.1.4",
    "ts-node": "^10.9.2"
  },
  "devDependencies": {
    "typescript": "^5.4.0"
  }
}
