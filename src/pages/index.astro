<a href={`/categories/${cat.slug}/`}
   class="group block rounded-xl border border-slate-200/80 bg-white p-4 shadow-sm hover:shadow-md transition-shadow"
   aria-label={`${cat.title} calculators`}>
  <div class="flex items-start gap-3">
    <img src={`/icons/${cat.icon}.svg`} width="28" height="28" alt="" aria-hidden="true" loading="lazy" />
    <div>
      <h3 class="text-lg font-semibold text-slate-900 group-hover:underline">{cat.title}</h3>
      <p class="text-sm text-slate-600">{cat.desc}</p>
    </div>
  </div>
</a>
