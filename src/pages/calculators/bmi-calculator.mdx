---
title: BMI Calculator
description: Body Mass Index from weight (kg) and height (m).
updated: "2025-08-21"
cluster: "Health"
---

import BaseLayout from "../../layouts/BaseLayout.astro";
import Calculator from "../../components/Calculator.astro";

export const schema = {
  inputs: [
    { name: "weight", label: "Weight (kg)", type: "number", placeholder: "Enter weight in kilograms", step: 0.1, min: 0 },
    { name: "height", label: "Height (m)", type: "number", placeholder: "Enter height in meters", step: 0.01, min: 0 }
  ],
  slug: "bmi-calculator",
  title: "BMI Calculator",
  locale: "en",
  expression: "weight / (height ^ 2)",
  intro: "Compute your body mass index (BMI).",
  examples: [{ description: "Example: weight 70 kg, height 1.75 m → 70 / (1.75^2) ≈ 22.857." }],
  faqs: [{ question: "How do I use it?", answer: "Enter your weight (kg) and height (m), then click Calculate." }],
  disclaimer: "Educational only."
};

<BaseLayout title={schema.title} description={schema.description}>
  <Calculator schema={schema} />
</BaseLayout>
