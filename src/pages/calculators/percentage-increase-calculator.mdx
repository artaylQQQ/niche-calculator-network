---
cluster: "Everyday"
title: "Percentage Increase Calculator"
description: "Increase an amount by a percentage."
updated: "2025-08-18"
---

import BaseLayout from "../../layouts/BaseLayout.astro";
import Calculator from "../../components/Calculator.astro";

export const schema = {
  "slug": "percentage-increase-calculator",
  "title": "Percentage Increase Calculator",
  "locale": "en",
  "inputs": [
    {"name":"original","type":"number","min":0,"step":0.01,"hint":"Original amount"},
    {/* removed expr */}
  ],
  "expression": "original * (1 + increase/100)",
  "disclaimer": "Educational information, not professional advice.",
  "schema_org": "FAQPage"
};

<BaseLayout title={schema.title} description={schema.description}>
  <Calculator schema={schema} />
</BaseLayout>
