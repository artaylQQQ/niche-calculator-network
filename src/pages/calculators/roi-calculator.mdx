---
cluster: "Health"

title: "ROI Calculator"
description: "Return on investment (percentage)."
updated: "2025-08-18"
---

import BaseLayout from "../../layouts/BaseLayout.astro";
import Calculator from "../../components/Calculator.astro";

export const schema = {
  "slug": "roi-calculator",
  "title": "ROI Calculator",
  "locale": "en",
  "inputs": [
    {"name":"gain","type":"number","min":0,"step":0.01,"hint":"Gain"},
    {"name":"cost","type":"number","min":0,"step":0.01,"hint":"Cost"}
  ],
  "expression": "((gain - cost) / cost) * 100",
  "disclaimer": "Educational information, not professional advice.",
  "schema_org": "FAQPage"
};

<BaseLayout title={schema.title} description={schema.description}>
  <!-- ad-slot: top -->
<Calculator schema={schema} />
</BaseLayout>

<!-- ad-slot: bottom -->
