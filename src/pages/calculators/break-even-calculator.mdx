---
title: Break-Even Units Calculator
description: Estimate units to break even.
updated: "2025-08-21"
cluster: "Finance"
---

import BaseLayout from "../../layouts/BaseLayout.astro";
import Calculator from "../../components/Calculator.astro";

export const schema = {
  inputs: [
    { name: "fixed", label: "Fixed Costs", type: "number", placeholder: "Enter fixed costs", step: 0.01, min: 0, units: "$" },
    { name: "price", label: "Price per unit", type: "number", placeholder: "Enter unit price", step: 0.01, min: 0, units: "$" },
    { name: "variable", label: "Variable cost per unit", type: "number", placeholder: "Enter variable cost per unit", step: 0.01, min: 0, units: "$" }
  ],
  slug: "break-even-calculator",
  title: "Break-Even Units Calculator",
  locale: "en",
  expression: "fixed / (price - variable)",
  intro: "Estimate how many units you need to sell to break even.",
  examples: [{ description: "Example: Fixed = $1000, Price = $50, Variable = $30 → 1000 / (50 - 30) = 50 units." }],
  faqs: [{ question: "How do I use it?", answer: "Enter fixed costs, unit price, and variable unit cost, then click Calculate." }],
  disclaimer: "Educational only."
};

<BaseLayout title={schema.title} description={schema.description}>
  <Calculator schema={schema} />
</BaseLayout>
