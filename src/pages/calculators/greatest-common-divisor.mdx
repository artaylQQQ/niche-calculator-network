---
layout: ../../layouts/CalculatorLayout.astro
title: "Greatest Common Divisor Calculator"
description: "Find the greatest common divisor (GCD) of two integers."
updated: "2025-02-14"
cluster: "Math"
---
import Calculator from "../../components/Calculator.astro";

export const schema = {
  slug: "greatest-common-divisor",
  title: "Greatest Common Divisor Calculator",
  locale: "en",
  intro: "Find the greatest common divisor (GCD) of two integers.",
  unit: "units",
  cluster: "Math",
  inputs: [
    { name: "a", label: "Number A", type: "number", step: "1", placeholder: "54" },
    { name: "b", label: "Number B", type: "number", step: "1", placeholder: "24" }
  ],
  expression: "((a,b)=>{a=Math.abs(a);b=Math.abs(b);while(b){[a,b]=[b,a%b]}return a})(a,b)",
  examples: [
    { description: "54 and 24 ⇒ 6" },
    { description: "81 and 57 ⇒ 3" }
  ],
  faqs: [
    { question: "What is GCD?", answer: "The largest integer that divides both numbers without remainder." },
    { question: "Can I input negative numbers?", answer: "Yes, absolute values are used." },
    { question: "What if one number is zero?", answer: "The GCD equals the absolute value of the other number." },
    { question: "Is this the Euclidean algorithm?", answer: "Yes, it uses the iterative Euclidean method." }
  ],
  disclaimer: "For arithmetic purposes; verify results for critical calculations.",
};

<Calculator schema={schema} />
