---
schema:
  title: "Circle Area Calculator"
  slug: "circle-area-calculator"
  intro: "Compute the area of a circle from its radius. Uses A = π · r²."
  inputs:
    - name: "radius"
      label: "Radius"
      type: "number"
      min: 0.000001
      step: 0.01
      placeholder: "e.g., 10"
      unit: "cm"
  expression: "3.141592653589793 * radius * radius"
  formula: "3.141592653589793 * radius * radius"
  examples:
    - title: "Radius 10 cm"
      inputs: { radius: 10 }
      result: "≈ 314.16 cm²"
  faqs:
    - q: "Why doesn’t it use Math.PI?"
      a: "The evaluator does not support Math.*, so we use a numeric literal."
  meta:
    title: "Circle Area Calculator — Compute A = π·r² Online"
    description: "Fast circle area calculator. Enter a radius to compute A = π·r² instantly."
---
