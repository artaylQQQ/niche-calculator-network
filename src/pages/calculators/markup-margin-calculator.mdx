---
title: Markup/Margin Calculator
description: Compute selling price from cost and margin.
updated: "2025-08-21"
cluster: "Finance"
---

import BaseLayout from "../../layouts/BaseLayout.astro";
import Calculator from "../../components/Calculator.astro";

export const schema = {
  inputs: [
    { name: "cost", label: "Cost", type: "number", placeholder: "Enter cost", step: 0.01, min: 0, units: "$" },
    { name: "margin", label: "Margin (%)", type: "number", placeholder: "Enter margin %", step: 0.01, min: 0, max: 99.999 }
  ],
  slug: "markup-margin-calculator",
  title: "Markup/Margin Calculator",
  locale: "en",
  expression: "cost / (1 - (margin / 100))",
  intro: "Compute selling price from cost and margin percentage.",
  examples: [{ description: "Example: cost $80, margin 25% → price = 80 / (1 - 0.25) = $106.667." }],
  faqs: [{ question: "How do I use it?", answer: "Enter cost and margin (%). The result is the selling price." }],
  disclaimer: "Educational only."
};

<BaseLayout title={schema.title} description={schema.description}>
  <Calculator schema={schema} />
</BaseLayout>
