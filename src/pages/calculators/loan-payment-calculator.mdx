---
cluster: "Everyday"
title: "Loan Payment Calculator"
description: "Compute fixed monthly payment for a loan."
updated: "2025-08-18"
---

import BaseLayout from "../../layouts/BaseLayout.astro";
import Calculator from "../../components/Calculator.astro";

export const schema = {
  "slug": "loan-payment-calculator",
  "title": "Loan Payment Calculator",
  "locale": "en",
  "inputs": [
    {"name":"principal","type":"number","min":0,"step":0.01,"hint":"Loan amount"},
    {/* removed expr */},
    {/* removed expr */}
  ],
  "intro": "Enter loan amount, annual interest, and term to get the fixed monthly payment.",
  "expression": "", 
  "disclaimer": "Educational information, not professional advice.",
  "schema_org": "FAQPage"
};

<BaseLayout title={schema.title} description={schema.description}>
  <Calculator schema={/* removed undefined var: schema */} />
</BaseLayout>
