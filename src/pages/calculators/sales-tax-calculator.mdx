---
cluster: "Everyday"
title: "Sales Tax Calculator"
description: "Final price after adding sales tax."
updated: "2025-08-18"
---

import BaseLayout from "../../layouts/BaseLayout.astro";
import Calculator from "../../components/Calculator.astro";

export const schema = {
  "slug": "sales-tax-calculator",
  "title": "Sales Tax Calculator",
  "locale": "en",
  "inputs": [
    {"name":"price","type":"number","min":0,"step":0.01,"hint":"Price"},
    {"name":"tax","type":"number","min":0,"step":0.01,"hint":"Tax (%)"}
  ],
  "expression": "price * (1 + tax/100)",
  "disclaimer": "Educational information, not professional advice.",
  "schema_org": "FAQPage"
};

<BaseLayout title={schema.title} description={schema.description}>
  <Calculator schema={schema} />
</BaseLayout>
