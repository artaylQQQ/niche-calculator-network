---
title: Simple Interest Calculator
description: Compute interest earned with simple interest.
updated: "2025-08-21"
cluster: "Finance"
---

import BaseLayout from "../../layouts/BaseLayout.astro";
import Calculator from "../../components/Calculator.astro";

export const schema = {
  inputs: [
    { name: "principal", label: "Principal", type: "number", placeholder: "Enter principal", step: 0.01, min: 0, units: "$" },
    { name: "rate", label: "Rate (%)", type: "number", placeholder: "Enter annual rate", step: 0.01, min: 0 },
    { name: "years", label: "Years", type: "number", placeholder: "Enter years", step: 1, min: 0 }
  ],
  slug: "simple-interest-calculator",
  title: "Simple Interest Calculator",
  locale: "en",
  expression: "(principal * rate * years) / 100",
  intro: "Compute interest earned with simple interest.",
  examples: [{ description: "Example: Principal $1000, Rate 5%, Years 2 → Interest = $100." }],
  faqs: [{ question: "How do I use it?", answer: "Enter principal, annual rate (%) and years. The result is the interest earned." }],
  disclaimer: "Educational only."
};

<BaseLayout title={schema.title} description={schema.description}>
  <Calculator schema={schema} />
</BaseLayout>
