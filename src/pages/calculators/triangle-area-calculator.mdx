---
layout: ../../layouts/BaseLayout.astro
title: "Triangle Area Calculator"
description: "Area from base and height."
updated: "2025-08-21"
cluster: "Everyday"
---

import Calculator from '../../components/Calculator.astro';

export const schema = {
  "inputs": [
    {
      "name": "base",
      "label": "Base",
      "type": "number",
      "placeholder": "Enter Base",
      "step": "any"
    },
    {
      "name": "height",
      "label": "Height (cm)",
      "type": "number",
      "placeholder": "Enter Height (cm)",
      "step": "any"
    }
  ],
  "slug": "triangle-area-calculator",
  "title": "Triangle Area Calculator",
  "locale": "en",
  "expression": "base",
  "intro": "Area from base and height.",
  "examples": [
    {
      "description": "Enter values and click Calculate."
    }
  ],
  "faqs": [
    {
      "question": "How do I use the Triangle Area Calculator?",
      "answer": "Enter the required values and press Calculate to get the result."
    }
  ],
  "disclaimer": "Educational information, not professional advice.",
  "cluster": "Everyday"
};

<Calculator schema={schema} />
