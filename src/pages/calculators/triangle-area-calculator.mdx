---
cluster: "Everyday"
title: "Triangle Area Calculator"
description: "Area from base and height."
updated: "2025-08-18"
---

import BaseLayout from "../../layouts/BaseLayout.astro";
import Calculator from "../../components/Calculator.astro";

export const schema = {inputs: [
  { name: "base", label: "Base", type: "number", placeholder: "Enter Base", step: "any" },
  { name: "height", label: "Height (cm)", type: "number", placeholder: "Enter Height (cm)", step: "any" }
],

  "slug": "triangle-area-calculator",
  "title": "Triangle Area Calculator",
  "locale": "en",
  "inputs": [
    {"name":"base","type":"number","min":0,"step":0.01,"hint":"Base"},
    
  ],
  "expression": "(base * height) / 2",
  "disclaimer": "Educational information, not professional advice.",
  "schema_org": "FAQPage"
};

<BaseLayout title={schema.title} description={schema.description}>
  <Calculator schema={schema} />
</BaseLayout>
