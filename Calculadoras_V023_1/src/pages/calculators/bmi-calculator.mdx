---
cluster: "Finance"

title: "BMI Calculator"
description: "Body Mass Index from weight and height."
updated: "2025-08-18"
---

import BaseLayout from "../../layouts/BaseLayout.astro";
import Calculator from "../../components/Calculator.astro";

export const schema = {
  "slug": "bmi-calculator",
  "title": "BMI Calculator",
  "locale": "en",
  "inputs": [
    {"name":"weight","type":"number","min":0,"step":0.1,"hint":"Weight (kg)"},
    {"name":"height","type":"number","min":0,"step":0.1,"hint":"Height (cm)"}
  ],
  "expression": "weight / ((height/100) ^ 2)",
  "disclaimer": "Educational information, not professional advice.",
  "schema_org": "FAQPage"
};

<BaseLayout title={schema.title} description={schema.description}>
  <!-- ad-slot: top -->
<Calculator schema={schema} />
</BaseLayout>

<!-- ad-slot: bottom -->
